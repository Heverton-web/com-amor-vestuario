export function numToWords(num: number): string {
  if (num === 0) return "zero";
  return String(num) + " reais"; // temporary stub
}

export function brlInWords(num: number): string {
  return String(num) + " reais"; // temporary stub
}
